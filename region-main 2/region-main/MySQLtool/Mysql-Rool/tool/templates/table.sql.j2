create table if not exists {{ table_name }} (
        id INT PRIMARY KEY,    
        roll_no INT NOT NULL,                
        name VARCHAR(100) NOT NULL,           
        class VARCHAR(50),                    
        subject VARCHAR(100) 
    );
